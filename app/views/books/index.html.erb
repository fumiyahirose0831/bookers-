<!DOCTYPE html>
<html lang="JP">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>bookers/books</title>
</head>
<body>
  <p id="notice"></p>
  <h1>Books</h1>
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
        <tr>
        <td>たのしいRuby</td>
        <td>Rubyの入門におすすめです</td>
        <td><a class="show_45854" href="/books/45854">Show</a></td>
        <td><a class="edit_45854" href="/books/45854/edit">Edit</a></td>
        <td><a data-confirm="Are you sure?" class="destroy_45854" rel="nofollow" data-method="delete" href="/books/45854">Destroy</a></td>
      </tr>
        <tr>
      <div class="field">
       <% @books.each do |book| %>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
       <% end %>
      </div>
	</tbody>
    </tbody>

  </table>
 <%= form_with model:@book, local:true do |f| %>
  <h2>New book</h2>

  <div class="field">
    <label for="book_title">Title</label>
     <%= f.text_field :title %>

  </div>

  <div class="field">
    <label for="book_body">Body</label>
    <%= f.text_area :body %>

  </div>

  <div class="actions">
      <%= f.submit 'Create Book' %>
  </div>
  <% end %>

</body>
</html>
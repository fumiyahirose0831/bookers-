<h1>Books#new</h1>
<p>Find me in app/views/books/new.html.erb</p>

 <% if @book.errors.any? %>
  <%= @bookt.errors.count %>件のエラーが発生しました
  <ul>
  <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
  </ul>
<% end %>
  
<h1>新規投稿</h1>
<!-- form_with部分 -->
<%= form_with model:@book, local:true do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>カテゴリー</h4>
  <%= f.text_field :category %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit '投稿' %>

<% end %>